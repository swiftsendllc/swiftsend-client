"use client";

import { Card, CardHeader, Container } from "@mui/material";

export default function ReelsScrollPage() {
  return (
    <Container maxWidth="xs">
      <Card>
        
        <CardHeader />
      </Card>
    </Container>
  );
}
